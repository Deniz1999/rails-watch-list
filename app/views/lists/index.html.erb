<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All the lists</h1>
    </div>
  </div>
</div>

<div class="row">
    <div class="col">
      <% @lists.each do |list| %>
          <%= link_to list.name, list_path(list) %>
      <% end %>
    </div>
</div>
