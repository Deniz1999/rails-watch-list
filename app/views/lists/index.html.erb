<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All the movies</h1>
    </div>
  </div>

  <div class="row">
    <div class="col">
      <% if @query %>
        <p>You searched for <strong><%= @query %></strong>. We found <%= @lists.count %> flats out of our <%= List.count %> on our website.</p>
      <% else %>
        <p>All <%= List.count %> flats.</p>
      <% end %>
    </div>
  </div>
</div>
